# Backend base URL used ONLY by the Vite dev-server proxy for /api and /ws-chat.
# Not needed in production if the built app is served by the same backend
# (the app always calls relative /api and /ws-chat paths).
VITE_DEV_HOST=localhost
VITE_DEV_PORT=5173

VITE_API_BASE_URL=http://localhost:8080
